server {
    listen              80;
    server_name         www.k8s-training.wizzie.io;

    gzip on;
    gzip_types text/plain application/xml;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
    }

}
